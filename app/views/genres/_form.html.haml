= form_with(model: genre, local: true) do |form|
  - if genre.errors.any?
    #error_explanation
      %h2
        = pluralize(genre.errors.count, "error")
        prohibited this genre from being saved:
      %ul
        - genre.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :name
    = form.text_field :name, id: :genre_name
  .field
    = form.label :description
    = form.text_field :description, id: :genre_description
  .actions
    = form.submit
