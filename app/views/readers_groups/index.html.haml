%h1 Readers Groups
%table
  %thead
    %tr
      %th Name
      %th Description
      %th{:colspan => "3"}
  %tbody
    - @readers_groups.each do |readers_group|
      %tr
        %td= readers_group.name
        %td= readers_group.description
        %td= link_to 'Show', readers_group
        %td= link_to 'Edit', edit_readers_group_path(readers_group)
        %td= link_to 'Destroy', readers_group, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Readers Group', new_readers_group_path
